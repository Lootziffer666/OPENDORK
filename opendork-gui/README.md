# OPENDORK GUI

This is a Taildrops-inspired OPENDORK dashboard UI built with Tailwind CDN.

## Run locally

```bash
cd opendork-gui
python3 -m http.server 8080
# open http://localhost:8080
```

The GUI is intentionally backend-agnostic for now and stores demo state in browser localStorage.
